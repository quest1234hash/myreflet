<!-- 
<%if(doc.length>0){

	var l =  0;
	   
   for(var i=0;i< doc.length;i++){
	   
	   %> -->
	   <style>
		.image_pdf{
			width:60px;height:70;color:red
		}
	</style>
	<div class="modal fade field-edit-modal" id="doc-img-Modal-download" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
	<div class="modal-dialog" role="document">
	 <div class="modal-content">
	   <div class="modal-body">
		 <button type="button" class="close" data-dismiss="modal" aria-label="Close">
		   <span aria-hidden="true">&times;</span>
		 </button>
		  <div>
			  <div class="outer-big-img">
				 
			 <%for(var h=0;h< doc.length;h++){
				
				%>
				<% if ( doc[h].type == "image" ) {%>
				 <div class="inner-box-img">
					 <div id="img_modal" class="modal-img-big">
					   <img id="download_image" src="<%=doc[h].body%>">
					 </div>
				
				 
			   
				 <div class="modal-img-big download-link" id="download_link">
				   <input type="hidden" id="src_val<%=h%>"value="<%=doc[h].body%>">
				   <!-- <a href=""class="download_class hide_click_<%=h%>" onclick="download_img('<%=h%>')"><i class="fa fa-download" aria-hidden="true"></i></a> -->
					 <center><span class="loader" id="loader_id<%=h%>"><img src="../assets/images/loader-3.gif"height="42" width="62"></span></center> 
				 </div>
				 </div>
				 <% }else if (doc[h].type == "video") {%>
						 <div class="inner-box-img">
							 <div id="img_modal" class="modal-img-big">
								 <video id="vid_modal" width="400" height="400" controls>

									 <source src="https://ipfs.io/ipfs/<%=doc[h].body%>" type="video/mp4">
								 </video>
							 </div>
						 </div>
				 <%}
				 else if (doc[h].type == "pdf") {%>
						 <div class="inner-box-img">
							 <div id="img_modal" class="modal-img-big">
								
							 <object id="" class="PDFdoc" width="450" height="400" type="application/pdf" data="https://ipfs.io/ipfs/<%=doc[h].body%>" ></object>

							 </div>
						 </div>
				 <%}%>
			 <%}%>
			</div>
		  </div>
	
		   
		</div>
	 </div>
	</div>
	</div>
	<!-- <% l++; }}%> -->
	<!-- <%for(var h=0;h< doc.length;h++){%>
		<% if ( doc[h].type == "image" ) {%>
			<div class="image_pdf<%=h%>">
					<img src="<%=doc[h].body%>" class="image_id">
	
			</div>
			<%}%>
	<%}%> -->
	<script type="text/javascript">
		$(document).ready( function () {
			$('.loader').hide();
	
					$('#doc-img-Modal-download').modal();
	
		})
	
	
		// function download_img(h_val){
			 
		// 				// $('.hide_click_'+h_val).hide();
		// 				// 	$('#loader_id'+h_val).show();
		// 				// alert(h_val)
	
		// 	var success = $('#src_val'+h_val).val();
	
	
			
		// 	// var url = success.replace(/^data:image\/[^;]+/, 'data:application/octet-stream');
	
		
		// 	// setTimeout(function() {
		// 		// $(".image_id").attr("src", success);

		// 		$('#loader_id'+h_val).hide();

		// 					var restorepage = $('body').html();
		// 			// $('.nodata').hide();
		// 			var printcontent = $('.image_pdf'+h_val).clone();
		// 			$('body').empty().html(printcontent);
		// 			window.print();
		// 			$('body').html(restorepage);
		// 									// }, 3000);
	
		
		// }
	</script>
	<!-- <script type="text/javascript">


		function download_img(h_val) {
			alert(h_val)
	
			var logo_url = $('#src_val' + h_val).val();
	
			getImgFromUrl(logo_url, function (img) {
					generatePDF(img);
				});
			
		}
		function getImgFromUrl(logo_url, callback) {
	
		  alert(logo_url)
		
		  var img = new Image();
			img.src = logo_url;
			img.onload = function () {
				callback(img);
			};
	  } 
	  function generatePDF(img){
		// alert("img")
		var width = img.width/4
		var height = img.height/4
	
		var options = {orientation: 'p', unit: 'mm'};
		var doc = new jsPDF(options);
		alert(doc)
		doc.addImage(img, 'JPEG', 0, 0,width,height);
		doc.save("certifieDoc.pdf");
	
		}
		
	</script> -->